{
  "name": "aegis-blockchain",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run ../scripts/deploy.js --network amoy",
    "deploy:voting": "node deploy-voting-simple.js",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^4.0.1",
    "ethers": "^6.15.0",
    "hardhat": "^3.0.6"
  }
}
